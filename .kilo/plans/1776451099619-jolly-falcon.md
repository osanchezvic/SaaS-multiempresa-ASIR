# Plan: Refactoring and Code Quality Improvements

## 1. Analysis and Discovery
- [x] Explore scripts in `scripts/` to identify potential bugs, lack of error handling, or security issues.
- [x] Review `docker-compose.yml` and `env.tpl` files for security best practices.
- [x] Assess `README.md` and `docs/` for accuracy and completeness.

## 2. Identified Issues & Proposed Fixes
- **Security:**
  - Remove hardcoded default passwords (`admin123`, `npm123`) from `infra/docker-compose.yml`.
  - Use external environment files for sensitive configuration.
- **Infrastructure:**
  - Replace `latest` image tags with specific versions to prevent breaking changes.
  - Review `/var/run/docker.sock` exposure in `portainer` and `watchtower`.
- **Documentation:**
  - Update `README.md` with instructions on setting up environment variables securely.

## 3. Implementation Steps
- [ ] Update `infra/docker-compose.yml` to remove insecure defaults.
- [ ] Pin image versions in all `docker-compose.yml` files found in `infra/` and `catalogo/`.
- [ ] Create/Update templates to require environment variable injection.
- [ ] Update `README.md` to guide users on securing their deployments.

## 4. Verification Plan
- [ ] Run `docker compose config` on modified files to ensure syntax correctness.
- [ ] Verify no secrets are committed to the repository.
