# Plan: Security Analysis and Authelia Integration Assessment

## Objective
Perform a deep analysis of the "SaaS-multiempresa-ASIR" codebase to identify code and security vulnerabilities, and evaluate the feasibility/impact of integrating Authelia for enhanced security.

## Completed Analysis
- **Bash Scripts:** Identified insecure plaintext credential storage (`scripts/funciones/utils.sh`) and command injection risks via shell variable interpolation in templates.
- **Admin Dashboard:** Found potential CSRF vulnerability on logout, lack of CSRF protection in login/actions, and hardcoded default credentials.
- **Infrastructure:** Identified hardcoded passwords in `infra/proxy/docker-compose.yml` (`npm123`) and exposed management ports (81) without secondary authentication.

## Implementation Roadmap (Authelia Integration)
1. **Infrastructure Hardening:**
   - Remove hardcoded credentials from `infra/proxy/docker-compose.yml` and replace with environment variable references (`.env`).
   - Implement CSRF protection for `admin-dashboard/index.php`.
2. **Authelia Deployment:**
   - Deploy `authelia` and `redis` (session storage) in `infra/docker-compose.yml`.
   - Configure Nginx Proxy Manager to use "Forward Auth" against Authelia for critical services (`admin-dashboard` and NPM management console).
3. **Security Refactoring:**
   - Transition credential management in `scripts/` to use a more secure storage or encrypted files.

## Timeline/Steps
1.  **Hardening:** Update `infra/proxy/docker-compose.yml` to use `.env` files for secrets.
2.  **Authelia Integration:** Modify `infra/docker-compose.yml` to include Authelia containers.
3.  **Proxy Configuration:** Update NPM configurations to point to Authelia.
4.  **PHP Updates:** Add CSRF tokens to `infra/admin-dashboard/index.php`.
5.  **Final Review:** Verify security posture of new configuration.

## Phase 4: Advanced Features for "A 10" Grade

### 3. API-driven Infrastructure
- **Development:** Create a lightweight API service (Python Flask/FastAPI) that acts as an interface to the existing shell scripts.
- **Endpoints:** 
  - `POST /deploy/<company>/<service>`: Triggers `./deploy.sh`.
  - `GET /status/<company>`: Returns status of deployed containers.
- **Integration:** Secure the API with internal auth tokens (Bearer tokens) and connect the `Admin Dashboard` to call these endpoints instead of manual CLI execution.

### 4. Automated Vulnerability Scanning
- **Implementation:** Install `trivy` binary on the deployment host.
- **Workflow:** Inject a "Pre-flight Security Check" step in `scripts/deploy.sh`.
- **Command:** `trivy image --exit-code 1 --severity CRITICAL <image_name>`.
- **Enforcement:** If a critical vulnerability is found, the deployment is aborted immediately and an error is logged.

### 5. Self-service Portal & RBAC
- **Database:** Alter `usuarios` table to add `empresa_id` (nullable for superadmins).
- **Access Control:** Implement middleware in `index.php` to filter queries:
  - If `role` == 'admin', show all.
  - If `role` == 'tenant_admin', `SELECT ... WHERE empresa_id = <logged_user_empresa_id>`.
- **UI:** Conditional rendering in the dashboard for `tenant_admin` (hide "global" stats, show only company-specific cards).

## Phase 5: Documentation & Professionalization
- **Repository README:** Rewrite for a professional presentation (Project objective, tech stack, setup instructions, security features).
- **Architecture Diagrams:** Standardize `docs/diagramas/` (C4 model or consistent style).
- **Defense Guide:** Review `docs/GUIA_DEFENSA.md` to ensure it highlights the security measures (Authelia, CSRF, Trivy) added during this project.
- **API Documentation:** Generate a simple OpenAPI/Swagger specification (or well-formatted Markdown) for the new API service.

